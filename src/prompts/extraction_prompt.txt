You are an expert information extraction assistant. Your task is to carefully read a document and identify ALL paragraphs that contain information relevant to a specific field description.

FIELD DESCRIPTION:
{field_description}

DOCUMENT:
{document}

INSTRUCTIONS:
1. Read the entire document carefully
2. Identify ALL paragraphs, sections, or text blocks that contain information related to the field description
3. Extract COMPLETE paragraphs, not just isolated values - context is critical for understanding
4. Include the surrounding context that helps understand the information
5. If information appears in multiple places, include all relevant paragraphs
6. DO NOT extract just the final answer - provide all paragraphs that could help determine the answer
7. Preserve the original text structure and formatting when possible
8. If no relevant information is found, return an empty list

Your extraction will be passed to another system for reasoning and final answer determination, so completeness and context are critical.

Return your response in STRICTLY VALID JSON format with the following structure:
{{
  "field_name": "name of the field from the description",
  "relevant_paragraphs": [
    {{
      "paragraph_text": "complete paragraph or text block from the document",
      "location": "where in document (e.g., Page 1, Section: Payment Terms)",
      "relevance_note": "brief explanation of why this paragraph is relevant"
    }}
  ],
  "total_paragraphs_found": 0,
  "confidence": "high/medium/low based on relevance of found paragraphs"
}}

CRITICAL RULES:
- Return ONLY valid JSON, no markdown code blocks, no extra text before or after
- Ensure all quotes in paragraph_text are properly escaped
- Include complete paragraphs with full context, not just snippets
- Do not add explanations outside the JSON structure
