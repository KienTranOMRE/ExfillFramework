# Core dependencies
python-dotenv
streamlit
requests

# OCR Services
google-generativeai  # Gemini OCR

# LLM Services
google-generativeai  # Gemini LLM (already included above)
openai               # Qwen (OpenAI-compatible API)
